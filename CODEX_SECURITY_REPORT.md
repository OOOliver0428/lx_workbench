# Codex Security 安全审查报告

> 状态：人工导出的已验证报告。Codex Security 的发现、验证和攻击路径阶段均已完成；官方最终封存器因 Windows GBK 解码错误未能生成封存版 `report.md`。

## 0.1.1 修复状态

- CS-01、CS-02、CS-03、CS-05 至 CS-14 已完成代码修复和回归测试。
- Linux 备份路径待验证项已通过 root 管理备份根目录、固定公开备份目标和无特权写入边界完成代码加固。
- `switch-db` 已限制为数据根目录直属文件，并使用 `openat`/`O_NOFOLLOW`、`fstat`、`fchown`、
  `fchmod` 处理 root 权限；当前 Windows 工作区完成了静态回归和 Bash 语法检查，正式 Ubuntu
  上线前仍须执行竞态与切换/回滚运行时验收。
- CS-04 密码长度与强度按产品决定暂不修改，列为正式生产服务器上线前阻断门禁。
- 0.1.1 回归结果：Python 77 项、前端 7 项、Ruff、ESLint、Bash 语法、锁文件与 Alembic head 检查通过。
- 本报告保留扫描时的原始风险描述，以上状态用于区分已修复项和明确延期项。

## 扫描摘要

| 项目 | 结果 |
|---|---|
| 仓库版本 | `9c879f60d343132fbc3f1e16c625256ceee57ca3` |
| 扫描范围 | 整个仓库 `.` |
| 审查清单 | 201/201，包含 145 个文本文件和 56 个 WebP 资源 |
| 发现候选 | 47 |
| 最终漏洞 | 14：中危 6，低危 8 |
| 被排除候选 | 32：产品明确策略、现有控制或不满足有效攻击边界 |
| 待验证项 | 1：Linux 备份路径 TOCTOU 竞态 |
| 自动化验证 | Python 测试 69 项全部通过；另完成节流状态增长、校验异常和弱密码的定向运行时检查 |

## 结论

最需要优先处理的是三个资源消耗边界：公开代理无请求体上限、登录 Argon2 缺少全局工作量限制、多个 LLM 入口缺少用户级配额和并发控制。密码修改允许五字符密码，以及可持续锁定指定账号，也应在同一批次修复。

本次没有确认远程代码执行、SQL 注入、任意文件读写、存储型 XSS、认证绕过或跨租户数据泄露。项目和商机的团队内全局可见性、`TEAM_LEADER` 的全局管理能力均有产品文档、前端权限逻辑和测试支持，因此未作为漏洞报告。

## 中危发现

### CS-01 轮换登录标识可绕过节流并持续触发 Argon2

- **CWE**：CWE-307、CWE-400
- **位置**：`app/api/routes/auth.py:38`、`app/api/routes/auth.py:50`、`app/throttle.py:39`
- **攻击路径**：未认证调用者持续更换不存在的登录标识，每次都会命中新节流键并执行固定工作量的 Argon2 校验。
- **影响**：在单后端实例上持续占用 CPU，降低认证接口及整个服务的可用性。
- **验证**：运行时检查确认新标识不会受到已有桶限制；代码中不存在来源级或应用级工作量预算。
- **修复**：在 Argon2 前增加来源级和应用级全局限流、并发上限及有界队列，同时保留账号维度节流。

### CS-02 AI 聊天缺少用户级并发、频率和预算限制

- **CWE**：CWE-400、CWE-770
- **位置**：`app/api/routes/ai.py:132`、`app/services/ai.py:519`
- **攻击路径**：具备 `AI_USE` 的用户并发或重复提交提示词，每次都构建数据库上下文并同步调用外部模型。
- **影响**：消耗共享模型额度、占用后端工作线程和数据库连接，影响其他用户。
- **现有控制**：CSRF、提示词长度和单次 `max_tokens` 只限制单次请求，不能限制累计消耗。
- **修复**：增加用户级速率、并发和 token/费用预算；在上下文构建前拒绝超限请求，并支持取消或去重在途调用。

### CS-03 公开代理在认证和超时前缓冲无限请求体

- **CWE**：CWE-400、CWE-770
- **位置**：`frontend/app/api/[...path]/route.ts:64`
- **攻击路径**：未认证调用者向任意非 GET/HEAD API 路径发送大请求体，代理先执行 `request.arrayBuffer()`，之后才创建上游超时和进入后端控制。
- **影响**：单个或少量大请求即可造成 Node 进程显著内存压力，严重时终止前端代理。
- **修复**：读取前校验 `Content-Length`，对分块请求使用有硬字节上限的流式读取，并在生产入口配置相同的请求体上限。

### CS-04 密码修改允许五字符弱密码

- **CWE**：CWE-521
- **位置**：`app/schemas.py:134`、`app/api/routes/auth.py:158`
- **攻击路径**：用户在首次强制改密或日常改密时可设置 `abcde` 等五字符密码，之后该密码可直接登录。
- **影响**：降低账号抵抗在线猜测和凭据填充的能力，影响所有角色账号。
- **验证**：现有测试及定向模型检查均确认五字符密码被接受；初始密码反而要求至少十字符。
- **修复**：统一初始密码和改密策略，拒绝纯空白、常见及已泄露密码，并保留 Argon2 与改密后的其他会话撤销。

### CS-05 未认证调用者可持续锁定指定账号

- **CWE**：CWE-645
- **位置**：`app/api/routes/auth.py:38`、`app/throttle.py:8`、`tests/test_security_hardening.py:20`
- **攻击路径**：攻击者针对已知登录标识提交五次错误密码，共享账号桶会在密码校验前阻止后续请求；攻击者可在每个五分钟窗口重复操作。
- **影响**：合法用户即使提供正确密码也无法登录，可形成低成本、持续的定向拒绝服务。
- **修复**：采用渐进延迟、风险信号、告警和恢复机制；将防 CPU 滥用的来源级限流与账号保护分离，避免硬性预校验锁定。

### CS-06 周报重新生成缺少频率和并发控制

- **CWE**：CWE-770
- **位置**：`app/api/routes/weekly_reports.py:63`、`app/services/weekly_reports.py:89`、`app/services/ai.py:515`
- **攻击路径**：默认具备周报维护权限的用户可重复或并发触发生成，每次均进行高 token 的同步模型调用。
- **影响**：单个普通用户即可消耗共享模型额度并占用服务资源。
- **修复**：增加用户/自然周维度的冷却和配额、在途生成锁、请求去重及供应商预算控制。

## 低危发现

### CS-07 登录节流状态不会全局清理

- **CWE**：CWE-400、CWE-770
- **位置**：`app/throttle.py:12`、`app/throttle.py:32`、`app/api/routes/auth.py:55`
- **证据**：定向检查写入 10,000 个唯一标识后，内存中保留了 20,000 个键；过期清理只在再次访问同一键时发生。
- **影响**：持续请求会逐步增加进程内存，但达到故障阈值需要较大请求量。
- **修复**：使用有容量限制的 TTL 缓存或全局清扫机制，并限制不同攻击者键的总数。

### CS-08 登录失败可导致审计数据库无限增长

- **CWE**：CWE-400、CWE-770
- **位置**：`app/api/routes/auth.py:55`、`app/api/routes/auth.py:59`、`app/audit.py:54`
- **攻击路径**：每个新登录标识都绕过现有节流并提交一条 SQLite 审计记录，仓库中没有保留期限或容量上限。
- **修复**：对失败事件聚合或采样，配置保留策略、磁盘监控和容量上限，并修复全局登录限流。

### CS-09 团队周报生成缺少幂等和配额控制

- **CWE**：CWE-400、CWE-770
- **位置**：`app/api/routes/dashboard.py:31`、`app/services/dashboard.py:1241`、`app/services/dashboard.py:1314`
- **影响**：具备团队周报权限的负责人可对同一周重复调用模型并持续插入新记录。
- **修复**：按生成者、团队和周建立唯一约束及在途锁，并增加用户和供应商配额。

### CS-10 root 安装器执行未校验的 uv 下载脚本

- **CWE**：CWE-494
- **位置**：`deploy/ubuntu/install.sh:269`
- **攻击路径**：uv 分发端或可信 TLS 路径被攻破时，恶意脚本会被直接传给 `sh` 并以 root 权限执行。
- **现有控制**：固定 HTTPS 地址降低了概率，但相邻 Node 下载路径已有 SHA-256 校验，uv 路径没有等效控制。
- **修复**：先下载到临时文件，校验固定版本的官方摘要或签名，验证成功后再执行。

### CS-11 switch-db 路径检查与 root 权限操作存在竞态

- **CWE**：CWE-59、CWE-367
- **位置**：`deploy/ubuntu/ops.sh:363`、`deploy/ubuntu/ops.sh:377`
- **攻击路径**：已控制 `APP_USER` 的攻击者可在路径包含性检查后替换 `DATA_DIR` 下的符号链接，诱使 root 的 `chown`/`chmod` 作用于其他目标。
- **限制**：需要服务账号已被攻破、运维人员执行指定切库操作及精确竞态，未在 Linux 上动态复现。
- **修复**：先停止服务，使用基于已打开目录描述符的 no-follow `open`/`fstat`/`fchown`/`fchmod`，避免再次按路径名解析。

### CS-12 登出失败仍被前端显示为已退出

- **CWE**：CWE-613
- **位置**：`frontend/app/workspace-app.tsx:69`、`app/api/routes/auth.py:120`
- **攻击路径**：网络或服务端登出失败时，前端吞掉异常并清除本地状态，但 HttpOnly Cookie 和服务端会话仍可能保持有效。
- **影响**：用户误以为会话已撤销，原浏览器或已持有该会话的一方仍可在有效期内复用。
- **修复**：服务端未确认撤销时显示可重试错误，不要宣告退出完成，并保留足够状态进行可靠重试。

### CS-13 AI 聊天上下文绕过项目和任务查看权限

- **CWE**：CWE-639、CWE-862
- **位置**：`app/api/routes/ai.py:132`、`app/services/ai_context.py:87`、`app/services/work_records.py:41`
- **攻击路径**：用户保留 `AI_USE`、但项目或任务查看权限被撤销后，AI 上下文仍会读取相关项目和同级任务，并将数据发送给外部模型。
- **影响**：绕过独立功能权限，泄露范围限于与当前用户有关的内部业务上下文。
- **修复**：上下文查询复用直接 API 的有效权限和对象级过滤；未授权部分不得进入提示词。

### CS-14 周报上下文绕过项目和任务查看权限

- **CWE**：CWE-862
- **位置**：`app/api/routes/weekly_reports.py:64`、`app/services/weekly_reports.py:100`、`app/services/ai_context.py:179`
- **攻击路径**：用户保留周报维护权限但失去项目/任务查看权限后，生成流程仍从本人工作记录的链接中加载描述、标签、阻塞项和结果并发送给模型。
- **影响**：在定制权限组合下泄露有限的关联业务数据，并可能把内容写回生成的周报草稿。
- **修复**：序列化前按当前有效项目、任务权限及对象授权过滤每类上下文字段。

## 待验证项

### Linux 备份目录祖先符号链接竞态

- **位置**：`deploy/ubuntu/ops.sh:138`、`deploy/ubuntu/ops.sh:150`
- **疑点**：`realpath` 包含性检查与 root `install -d` 之间存在检查/使用间隔，已控制 `APP_USER` 的攻击者可能替换应用拥有的祖先目录。
- **缺口**：需要在受支持的 Linux/coreutils 环境中证明，竞态能够产生备份根目录之外的特权文件系统效果。

## 验证与限制

- 仓库现有 Python 测试：69 项全部通过。
- 定向运行时检查确认：10,000 个唯一登录标识留下 20,000 个节流键；自定义校验错误含不可 JSON 序列化的 `ValueError`；五字符新密码被模型接受。
- 未调用真实外部 LLM 服务，未消耗供应商额度。
- 未执行前端构建和浏览器级运行时验证；前端发现基于源代码和接口数据流分析。
- 未在 Linux 上动态复现两个运维脚本竞态，其中 `switch-db` 路径已作为低危报告，备份路径保留为待验证。

## 建议修复顺序

1. 为前端代理增加严格请求体上限。
2. 重构登录防护：全局 Argon2 工作量限制、有界节流状态、避免账号硬锁定、审计保留策略。
3. 统一密码策略并补充弱密码回归测试。
4. 为 AI 聊天、个人周报和团队周报统一增加配额、并发锁和幂等控制。
5. 在 AI/周报上下文构建层统一复用权限过滤。
6. 加固 Ubuntu 安装和数据库切换流程，并在 Linux 上完成竞态测试。
