# 更新日志

本文件记录所有面向用户、部署、配置、数据、安全和兼容性的有效变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning](https://semver.org/lang/zh-CN/)。维护规则见
[版本发布规范](docs/RELEASING.md)。

## [Unreleased]

### Added

### Changed

### Fixed

- 修复 Ubuntu 更新器以受限 `umask` 拉取新提交时，替换后的 Python 文件可能仅 root 可读，导致后端
  在导入阶段报 `PermissionError`、健康检查持续显示 8787 不可达的问题。

### Security

### Operations

- 安装和更新在服务启动前统一归一化发布树权限，并由后端服务账号检查应用源码可读性；部署手册补充
  该故障的最小化恢复流程，避免误回滚已成功迁移的数据库。

## [0.1.0] - 2026-08-04

首个可部署 MVP 版本，适用于 Ubuntu 单节点、受控内网试运行。

### Added

- 作战台：商机清单和关系图、工作管理、跨周趋势、周报提交矩阵和阶段时间线。
- 独立商机档案与追加式推进记录；方案交流阶段可预填创建项目，并原子关联唯一项目。
- 项目标签、父子层级、标准名称和别名、精确/模糊查重、合并预览及事务化合并。
- 任务分派、转派、协作者、优先级、阻塞、完成、取消和跨项目关联。
- 工作记录、待归集、本周筛选、项目/任务关联、风险、下一步行动和产出物链接。
- 个人周报、团队周报，以及基于当前用户可见数据的 AI 助手。
- DeepSeek、GLM、Kimi、MiniMax 多厂商配置、连接测试和加密密钥存储。
- 本地账号、首次密码修改、预置头像、服务端 Session、CSRF、递增权限、乐观锁、软删除和操作审计。
- 可重复执行的全功能演示数据。

### Changed

- 商机、项目和任务权限改为递增关系：查看 → 录入/编辑 → 新建/创建。
- 新账号默认权限调整为项目/任务只读、工作记录和个人周报全部功能、AI 助手及个人设置。
- 商机推进清单精简右侧信息，只展示人员，不再展示关联与工时。
- 商机关系图取消滚轮缩放，并以圆角矩形展示项目。

### Fixed

- 修复 HTTP 内网环境不提供 `crypto.randomUUID()` 时，AI 助手点击发送无请求的问题。
- 修复 Ubuntu 运维命令在固定系统 PATH 中找不到 `runuser` 的兼容问题，并明确依赖 `util-linux`。
- 修复旧升级流程可能遗漏前端 `devDependencies`、构建产物权限不足和运行中
  `node_modules` 被替换后前端无法启动的问题。

### Security

- 大模型 API Key 仅由后端加密保存，浏览器不持久化或回显密钥。
- 前后端使用隔离服务账号；前端账号不能读取运行配置和大模型加密主密钥。
- Ubuntu 发布目录由 root 持有，后端仅监听回环地址，SQLite 数据库限制在本机受控目录。
- 更新、迁移、切库和备份使用全局互斥锁，并在变更前生成可校验回滚快照。

### Operations

- 提供 Ubuntu 一键安装、systemd 服务、统一运维命令、每日 SQLite 在线备份和健康检查。
- 支持 GitHub fast-forward 更新，以及 GitHub 不可达时通过校验过的增量 Git bundle 更新。
- 支持试用库切空正式库、冻结提升试用库、外部 SQLite 校验切换和版本感知回滚。
- 增加前后端端口分层排障、备份 timer 复核、AI 厂商状态码定位和上线检查清单。
- 建立版本号一致性测试和合并请求发布检查，要求后续迭代持续维护 `Unreleased`。
