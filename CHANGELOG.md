# 更新日志

本文件记录所有面向用户、部署、配置、数据、安全和兼容性的有效变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning](https://semver.org/lang/zh-CN/)。维护规则见
[版本发布规范](docs/RELEASING.md)。

## [Unreleased]

本条目对应长期路径 A：将 GitHub `dev`（时间块、AI 多轮历史与上下文化）合入已含方案 B
（多部门负责人）的 `main`。两边能力同时保留，合入后的 tip 可与现网 `eedd4f28` 再做
merge-base 对比。

### Added

- 工作记录支持时间块圈选录入：创建、编辑和快速创建可提交 `time_blocks`（当天 00:00 起的
  半小时间隔，服务端以区间总时长覆盖 `minutes`）；工作记录页与快速创建接入时间块选择器。
- 工作记录时间块选择器支持「当天已占用」灰色背景提示：`GET /api/v1/work-records/occupancy`
  返回当前登录用户在指定日期的已占区间（编辑时可 `exclude_record_id` 排除本条）；仅作视觉
  提示，仍允许并行任务重叠圈选与保存。
- AI 助手按用户持久化多轮对话历史，并提供 `GET/DELETE /api/v1/ai/chat/history`；历史受
  消息数与字符双预算裁剪，最旧内容先丢弃。
- AI 业务上下文人话化：负责人与关联对象以名称互指、状态使用中文标签、带上海时区日期锚点；
  各分段除数量上限外还受总字符预算约束，被截断的分段写入 `truncated_sections`。

### Changed

- 同一用户可被设为多个部门的 `Department.leader_id`；其对主部门与所负责部门的部门工作、任务
  和工作记录拥有与成员相当的可见/可写/可创建/可任 owner 范围。用户仍只有一个主部门，直属
  Leader / 周报 / 作战台团队逻辑不变。AI 聊天上下文中的部门工作范围同步为主部门 ∪ 所负责
  的有效部门。
- 大模型接入配置不再预填厂商默认模型名称：切换服务商或接入方式时模型输入框清空，由用户
  自行填写；datalist 参考列表保留；打开页面时仍回显与当前厂商/接入方式一致的已保存模型。
- 工作记录卡片在合计工时旁展示已圈选的时间段（如 `09:00–11:00 · 14:00–15:30`）；无时间块
  的旧记录仍只显示工时。

### Fixed

- 修复 DeepSeek 聊天偶发返回空回复的问题。

### Security

### Operations

- 新增 3 个 Alembic 迁移，随服务启动前的 `alembic upgrade head` 执行：
  `d1f2a3b4c5e6`（`ai_chat_messages` 对话历史表）、`f1a6b7c8d902`（`work_record_time_blocks`
  时间块表）、`a6e1c3f9b204`（合并上述两个 head）。新表不影响旧版本代码读取；若回滚到不含
  这些能力的代码，需先 `alembic downgrade` 到 `c8a4d7e2f906`。
- 可选新环境变量（均有默认值，不改 `.env` 亦可运行）：`MVP_LLM_CHAT_HISTORY_MAX_MESSAGES`、
  `MVP_LLM_CHAT_HISTORY_MAX_CHARS`、`MVP_LLM_CHAT_MESSAGE_MAX_CHARS`、
  `MVP_LLM_CHAT_CONTEXT_MAX_CHARS`。无新增端口、systemd 或部署依赖。
- `dev` 合入时删除了根目录 `生成离线升级包.cmd` 包装脚本（`scripts/New-OfflineUpdateBundle.ps1`
  仍保留）。README 与 Ubuntu 部署文档仍提及该 cmd，后续需对齐文档或恢复包装脚本。

## [0.2.1] - 2026-08-14

首个 0.2 稳定版：将 0.2.0 beta 期间的部门工作模块、任务视图重构和工作记录快速录入合并至发布
主线 `main`。发布分支由 `mvp` 迁移为 `main`，旧原型主线冻结为只读分支 `prototype`，后续开发在
`develop` 进行，服务器只部署带版本标签的 `main` 提交。

### Added

- 部门目录与部门工作模块：部门负责人、公开/部门内可见范围、部门工作来源任务和归档只读。
- 任务视图重构：三级任务树、协作者、任务进度跟踪、转派历史、跨项目任务关联画布与树形完成/删除。
- 工作记录快速录入：一次请求可同时创建项目/部门工作/任务与工作记录，幂等键防重。
- Windows 离线升级包工具 `scripts/New-OfflineUpdateBundle.ps1` 与根目录 `生成离线升级包.cmd`。

### Changed

- 前端升级 Vinext 运行时，重构任务、工作记录和作战台视图与交互动效。
- 发布分支由 `mvp` 迁移为 `main`：安装器、更新命令、离线升级工具和文档的默认分支全部改为
  `main`，安装器同时接受 `main` 分支或版本标签检出的源码；`mvp` 分支停止使用。

### Fixed

- 修复切换周期存在的锚点漂移问题，以及前端升级后任务视图、工作记录和作战台的多项交互与显示问题。

### Security

- 无新增安全变更，沿用 0.1.1 安全加固基线；AI 聊天与周报上下文继续按当前用户可见范围过滤。

### Operations

- 新增 1 个 Alembic 迁移 `c8a4d7e2f906_add_department_work_task_workflow`（部门目录、部门工作与
  部门工作来源任务），随服务启动前的 `alembic upgrade head` 自动执行；新增表结构不影响旧版本
  代码运行，无需回滚数据库即可回滚旧代码。
- 无新增环境变量；`app.env`、数据库与备份格式不变。
- 常规维护窗口更新（停服 → 备份 → fast-forward → 构建 → 迁移 → 健康检查）；在线升级无需额外
  依赖缓存或制品，离线场景沿用增量 bundle 流程。
- 分支策略变更：`main` 为发布主线，`develop` 为开发分支，`prototype` 为冻结的旧原型主线。

## [0.1.1] - 2026-08-06

面向受控试运行环境的安全加固版本；密码长度与强度策略保持 0.1.0 行为，正式生产上线前必须另行收紧。

### Added

- 增加后端和前端代理请求体上限、登录 Argon2 全局工作量门控，以及统一的 LLM 配额、并发和冷却控制。
- 增加团队周报生成唯一约束迁移，聊天请求支持客户端请求 ID 和在途去重。

### Changed

- AI 聊天和周报上下文按当前有效项目、任务、工作记录权限逐类过滤，并限制上下文行数和标签数量。
- 登录失败改为有界 TTL 状态和采样审计；错误尝试不再预锁定账号，正确密码可立即恢复登录。
- 团队周报对同一生成者和自然周原位更新，不再重复插入历史行。

### Fixed

- 修复登出失败时前端仍清除本地状态、误报退出成功的问题。
- 修复 Ubuntu 更新器以受限 `umask` 拉取新提交时，替换后的 Python 文件可能仅 root 可读的问题。

### Security

- 限制公开代理请求体、登录密码校验并发、失败审计容量和三个模型生成入口的累计资源消耗。
- 修复 AI/周报上下文绕过项目和任务查看权限的问题。
- `uv` 改为校验固定版本官方二进制 SHA-256 后安装；切库在停服后重新检查路径和符号链接。

### Operations

- 备份根目录改为 root 管理，定时备份写入 `scheduled` 子目录；运维命令不再接受自定义备份路径。
- 安装和更新在服务启动前统一归一化发布树权限，并由后端服务账号检查应用源码可读性。

## [0.1.0] - 2026-08-04

首个可部署 MVP 版本，适用于 Ubuntu 单节点、受控内网试运行。

### Added

- 作战台：商机清单和关系图、工作管理、跨周趋势、周报提交矩阵和阶段时间线。
- 独立商机档案与追加式推进记录；方案交流阶段可预填创建项目，并原子关联唯一项目。
- 项目标签、父子层级、标准名称和别名、精确/模糊查重、合并预览及事务化合并。
- 任务分派、转派、协作者、优先级、阻塞、完成、取消和跨项目关联。
- 工作记录、待归集、本周筛选、项目/任务关联、风险、下一步行动和产出物链接。
- 个人周报、团队周报，以及基于当前用户可见数据的 AI 助手。
- DeepSeek、GLM、Kimi、MiniMax 多厂商配置、连接测试和加密密钥存储。
- 本地账号、首次密码修改、预置头像、服务端 Session、CSRF、递增权限、乐观锁、软删除和操作审计。
- 可重复执行的全功能演示数据。

### Changed

- 商机、项目和任务权限改为递增关系：查看 → 录入/编辑 → 新建/创建。
- 新账号默认权限调整为项目/任务只读、工作记录和个人周报全部功能、AI 助手及个人设置。
- 商机推进清单精简右侧信息，只展示人员，不再展示关联与工时。
- 商机关系图取消滚轮缩放，并以圆角矩形展示项目。

### Fixed

- 修复 HTTP 内网环境不提供 `crypto.randomUUID()` 时，AI 助手点击发送无请求的问题。
- 修复 Ubuntu 运维命令在固定系统 PATH 中找不到 `runuser` 的兼容问题，并明确依赖 `util-linux`。
- 修复旧升级流程可能遗漏前端 `devDependencies`、构建产物权限不足和运行中
  `node_modules` 被替换后前端无法启动的问题。

### Security

- 大模型 API Key 仅由后端加密保存，浏览器不持久化或回显密钥。
- 前后端使用隔离服务账号；前端账号不能读取运行配置和大模型加密主密钥。
- Ubuntu 发布目录由 root 持有，后端仅监听回环地址，SQLite 数据库限制在本机受控目录。
- 更新、迁移、切库和备份使用全局互斥锁，并在变更前生成可校验回滚快照。

### Operations

- 提供 Ubuntu 一键安装、systemd 服务、统一运维命令、每日 SQLite 在线备份和健康检查。
- 支持 GitHub fast-forward 更新，以及 GitHub 不可达时通过校验过的增量 Git bundle 更新。
- 支持试用库切空正式库、冻结提升试用库、外部 SQLite 校验切换和版本感知回滚。
- 增加前后端端口分层排障、备份 timer 复核、AI 厂商状态码定位和上线检查清单。
- 建立版本号一致性测试和合并请求发布检查，要求后续迭代持续维护 `Unreleased`。
