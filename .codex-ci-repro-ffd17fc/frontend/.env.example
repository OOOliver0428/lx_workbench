# Keep browser API calls same-origin so the HttpOnly session cookie is reliable.
NEXT_PUBLIC_API_BASE_URL=

# Server-side upstream used by the frontend rewrite.
MVP_INTERNAL_API_BASE_URL=http://127.0.0.1:8787
