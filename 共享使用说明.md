# 解决方案部门周报：共享使用说明

## 启动

双击 `启动周报服务.command`，保持弹出的终端窗口运行。

终端会显示两个地址：

- 本机使用：`http://127.0.0.1:8787`
- 同一办公网络的其他人使用：`http://本机局域网IP:8787`

所有人访问后看到的是同一份数据。不要再把 `file://.../index.html` 当作共享链接发送。

## 第一次迁移浏览器旧数据

1. 先启动周报服务。
2. 刷新原来通过 `file://.../index.html` 打开的页面。
3. 页面会将当前浏览器里的旧周报自动写入 SQLite 数据库。
4. 再打开 `http://127.0.0.1:8787` 检查数据。

如果旧页面已经无法打开，也可以使用页面中的“数据迁移”功能：先导出 JSON，再在共享网页中导入。

## 数据与备份

- 数据库文件：`weekly_report.db`
- 自动备份目录：`backups/`
- 每周日 23:50，服务器自动汇总当周部门周报。
- 每周一 00:10，服务器使用 SQLite 安全备份机制生成 `.db` 和 `.json` 两份备份；其中 JSON 可直接在“数据迁移”中重复导入。
- 如果电脑在计划时间休眠或服务未运行，恢复运行后会补做最近一次汇总和备份。
- 也可以继续使用网页里的 JSON 导出功能；导出文件支持重复导入，同一周事项不会重复累加。
- 只要服务运行、电脑未休眠且网络可达，其他设备就能访问。
- 这是局域网轻量方案，默认没有账号和权限控制。知道地址的人都可以浏览和修改数据。

## 个人周报录入格式

每条事项需同时填写交付物和工时，推荐直接整段粘贴：

```text
1、完成华东政务项目总体方案评审｜交付物：总体方案 V3、评审问题清单｜工时：8小时
2、完成智能办公场景演示｜交付物：演示环境、客户反馈纪要｜工时：4.5小时
```

系统会自动拆分事项、识别项目，并按“项目—人员”汇总交付物和投入工时。旧周报中没有工时的数据仍可查看，会标记为“历史数据未填写”。

## 手动启动或更换端口

```bash
cd "/Users/jaggu/Documents/解决方案部门周报"
python3 server.py
```

如 8787 端口被占用：

```bash
python3 server.py --port 8790
```

如需调整定时任务时间：

```bash
python3 server.py --summary-time 22:30 --backup-time 08:00
```
